# keeping this md for documentation purposes
- getComputerChoice() - gets the computer's choice by using Math.random and multiplying with array length
- playRound(playerSelection, computerSelection) - to determine player's selection and computer selection and use boolean operators. Increments the score for either the player or computer depending on the condition
- game() - loops the playround() 5 times and provides an alert with winner of current round and prints the final winner